ARG PG_VERSION="16"
ARG PG_PORT="5432"

# --- Config ---
CONFIG set allow.sysvipc 1
CONFIG set sysvshm new
CONFIG set sysvsem new
RESTART

# install required packages

PKG postgresql${PG_VERSION}-server

# enable and start postgre

SYSRC postgresql_enable=YES
SERVICE postgresql initdb

CMD sed -i '' "s%#listen_addresses = 'localhost'%listen_addresses = '${JAIL_IP}'%g" /var/db/postgres/data${PG_VERSION}/postgresql.conf

SERVICE postgresql start

#RDR tcp ${PG_PORT} 5432

#TAGS database postgresql pg${PG_VERSION}
